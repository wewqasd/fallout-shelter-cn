# 临时文件
*.pyc
__pycache__/
/tmp/

# 构建产物 / 大二进制（成品/差分/EXE 走 GitHub Release，不入库）
build/
.hdiff_lib/
Fallout Shelter/
.pylibs/
# PyInstaller 输出目录（Windows 打包产物走 Release）
dist/

# 参考资料与第三方克隆(不入库)
对比文件夹/

# 翻译表历史备份(不入库)
data/*.bak*
data/*.bak*/
